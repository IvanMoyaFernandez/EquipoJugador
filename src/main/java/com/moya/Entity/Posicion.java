package com.moya.Entity;

// No se va a utilizar, ya que la posición del jugador la añadimos mediante
// un STRING y no mediante una clase ENUM, como sería lo que vemos a continuación.

public enum Posicion {
    PIVOT, ALERO, BASE, ESCOLTA, ALA_PIVOT
}
